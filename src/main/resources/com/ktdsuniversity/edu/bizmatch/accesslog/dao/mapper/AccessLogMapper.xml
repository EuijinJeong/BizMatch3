<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktdsuniversity.edu.bizmatch.accesslog.dao.AccessLogDao">
  
  <insert id="insertNewAccessLog"
  parameterType="com.ktdsuniversity.edu.bizmatch.accesslog.vo.AccessLogVO">
  INSERT INTO ACCSS_LOG
	( ACCESS_LOG_ID
	, ACCESS_TYPE
	, ACCESS_EMAIL
	, ACCESS_TIME
	, ACCESS_URL
	, ACCESS_METHOD
	, ACCESS_IP
	, LOGIN_SUCCESS_YN)
	VALUES
	  ( 'AL-' || TO_CHAR(NOW(), 'YYYYMMDD') || '-' || LPAD(NEXTVAL('seq_access_log_pk')::text, 6, '0')
	  , #{accessType}
	  , #{accessEmail}
	  , NOW()
	  , #{accessUrl}
	  , #{accessMethod}
	  , #{accessIp}
	  , COALESCE(#{loginSuccessYn}, 'N'))
  </insert>

  <select id="selectLoginFailCount" resultType="_int" parameterType="string">
  SELECT COUNT(1)
	  FROM ACCSS_LOG
	 WHERE ACCESS_IP = #{_parameter}
	   AND ACCESS_TYPE ='LOGIN'
	   AND LOGIN_SUCCESS_YN = 'N'
	   AND ACCESS_TIME BETWEEN NOW() - INTERVAL '1 hour' AND NOW()
  </select>
</mapper>